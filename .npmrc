# Expo / React Native need a flat (hoisted) node_modules so Metro can resolve packages.
node-linker=hoisted
# Avoid ERR_PNPM_MISSING_TIME from registry metadata gaps in RN's dep tree.
resolution-mode=highest
