## What & why
Briefly describe what this PR changes and the reason for it.

## Related issue
Closes #

## Type of change
- [ ] Bug fix
- [ ] New feature
- [ ] Docs
- [ ] Refactor / chore

## Checklist
- [ ] Conventional commit messages (`feat(scope):`, `fix(scope):`, `chore:`)
- [ ] `pnpm tsc` and `pnpm test` pass (no host `npm`)
- [ ] `VERSION` == `app.json` expo.version == `package.json` version (if bumping)
- [ ] Brand values pulled from `src/theme/tokens.ts` (no ad-hoc hex)
